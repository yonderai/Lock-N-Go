class AppConstants {
  // Change this to your machine's IP when testing on real device
  // For emulator use 10.0.2.2, for real device use your local IP e.g. 192.168.1.5
  static const String baseUrl = "http://10.0.2.2:8000";
}